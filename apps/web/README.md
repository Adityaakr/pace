# PACE DAO Web App

This package contains the web client (`@pace-dao/web`) built with Expo + React Native Web.

## Run

From monorepo root:

```bash
pnpm dev
```

From this directory:

```bash
pnpm dev
```
